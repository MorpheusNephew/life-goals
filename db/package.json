{
  "name": "db",
  "version": "1.0.0",
  "description": "Scripts to run postgres",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "create-container": "docker container create --name life-goals-local -e MONGO_INITDB_ROOT_USERNAME=mongoUser -e MONGO_INITDB_ROOT_PASSWORD=mongoPassword -p 27017:27017 mongo",
    "start-container": "docker container start life-goals-local",
    "stop-container": "docker container stop life-goals-local",
    "remove-container": "docker container rm life-goals-local"
  }
}
