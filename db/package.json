{
  "name": "db",
  "version": "1.0.0",
  "description": "Scripts to run postgres",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "docker run --name life-goals-local -e POSTGRES_USER=POSTY_USER -e POSTGRES_PASSWORD=POSTY_PASSWORD -e POSTGRES_DB=LIFE_GOALS -p 5432:5432 postgres:15.2"
  }
}
